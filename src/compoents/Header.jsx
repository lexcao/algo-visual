import React from 'react'

export default ({ title }) => (
  <>
    <header className="w-full flex justify-center items-center">
      <span className="text-5xl p-8">{title}</span>
    </header>
  </>
)
